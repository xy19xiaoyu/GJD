<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="field">
      类型：<br>
      <%= select(:order,:Type, [['采购',"1"], ['销售', "2"]]) %>

    </div>
  <div class="field">
    订单编号<br>
    <%= f.text_field :OrderId %>
  </div>
  <div class="field">
    名称<br>
    <%= f.text_field :Name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
