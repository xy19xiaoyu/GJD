<%= form_for(@item_out_order) do |f| %>
  <% if @item_out_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_out_order.errors.count, "error") %> prohibited this item_out_order from being saved:</h2>

      <ul>
      <% @item_out_order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br>
    <%= f.text_field :id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
